<form method="post" action="{{ path('chess_game_delete', {'id': chess_game.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ chess_game.id) }}">
    <button class="btn">Delete</button>
</form>
